# Copia este archivo a ".env.local" (que YA está en .gitignore) y rellena tus valores.
# Esto es SOLO para pruebas locales con `npm run test:local`.
# En AWS Lambda (producción), estos valores NUNCA vienen de aquí:
# se leen dinámicamente desde AWS Secrets Manager (ver src/config.mjs).

NODE_ENV=local
SUPABASE_URL=https://gmmztxqokjvyzylmepwb.supabase.co
SUPABASE_SERVICE_ROLE_KEY=pega-aqui-tu-service-role-key
